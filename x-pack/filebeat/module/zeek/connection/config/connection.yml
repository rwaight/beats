type: log
paths:
{{ range $i, $path := .paths }}
 - {{$path}}
{{ end }}
exclude_files: [".gz$"]
tags: {{.tags | tojson}}
publisher_pipeline.disable_host: {{ inList .tags "forwarded" }}

json.keys_under_root: false

processors:
  - drop_fields:
      fields: ["json.orig_bytes","json.resp_bytes","json.tunnel_parents"]
      ignore_missing: true
  - rename:
      fields:
        - from: "json"
          to: "zeek.conn"

        - from: "zeek.conn.duration"
          to: "temp.duration"

        - from: "zeek.conn.id.orig_h"
          to: "source.address"

        - from: "zeek.conn.id.orig_p"
          to: "source.port"

        - from: "zeek.conn.id.resp_h"
          to: "destination.address"

        - from: "zeek.conn.id.resp_p"
          to: "destination.port"

        - from: "zeek.conn.proto"
          to: "network.transport"

        - from: "zeek.conn.service"
          to: "network.protocol"

        - from: "zeek.conn.uid"
          to: "zeek.session_id"

        - from: "zeek.conn.orig_ip_bytes"
          to: "source.bytes"

        - from: "zeek.conn.resp_ip_bytes"
          to: "destination.bytes"

        - from: "zeek.conn.orig_pkts"
          to: "source.packets"

        - from: "zeek.conn.resp_pkts"
          to: "destination.packets"

        - from: "zeek.conn.conn_state"
          to: "zeek.conn.state"

        - from: "zeek.conn.orig_l2_addr"
          to: "source.mac"

        - from: "zeek.conn.resp_l2_addr"
          to: "destination.mac"

      ignore_missing: true
      fail_on_error: false

  - rename:
      when.equals.network.transport: icmp
      fields:
        - from: "source.port"
          to: "zeek.conn.icmp.type"

        - from: "destination.port"
          to: "zeek.conn.icmp.code"

      ignore_missing: true
      fail_on_error: false
  - convert:
      fields:
        - {from: "zeek.session_id", to: "event.id"}
        - {from: "source.address", to: "source.ip", type: "ip"}
        - {from: "destination.address", to: "destination.ip", type: "ip"}
      ignore_missing: true
      fail_on_error: false
  - add_fields:
      target: event
      fields:
        kind: event
        category:
          - network
  - if:
      equals.network.transport: icmp
    then:
      community_id:
        fields:
          icmp_type: zeek.conn.icmp.type
          icmp_code: zeek.conn.icmp.code
    else:
      community_id:
{{ if .internal_networks }}
  - add_network_direction:
      source: source.ip
      destination: destination.ip
      target: network.direction
      internal_networks: {{ .internal_networks | tojson }}
{{ end }}
  - add_fields:
      target: ''
      fields:
        ecs.version: 1.12.0
